apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ourlittletribe-tls
  namespace: ourlittletribe
spec:
  secretName: ourlittletribe-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - "<MAGIC_DOMAIN>"
